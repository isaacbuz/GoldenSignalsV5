# GoldenSignalsAI Environment Configuration Template
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env in the backend directory
# 2. Fill in your actual values (NEVER commit .env to git)
# 3. Generate secure keys using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 
# ⚠️  SECURITY WARNING: NEVER commit .env files to version control!

# ============= CRITICAL SECURITY =============
# Generate strong keys: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_THIS_USE_SCRIPT_ABOVE
JWT_SECRET_KEY=CHANGE_THIS_USE_SCRIPT_ABOVE

# ============= DATABASE =============
# For production, use PostgreSQL with SSL
DATABASE_URL=postgresql+asyncpg://user:pass@localhost/dbname?ssl=require

# ============= API KEYS =============
# Get your keys from the providers' websites
# Store in environment variables or secret manager in production

# AI/LLM APIs (Optional but recommended)
OPENAI_API_KEY=sk-...  # https://platform.openai.com/api-keys
ANTHROPIC_API_KEY=sk-ant-...  # https://console.anthropic.com/

# Market Data APIs (Need at least one)
POLYGON_API_KEY=  # https://polygon.io/dashboard/api-keys
ALPHA_VANTAGE_API_KEY=  # https://www.alphavantage.co/support/#api-key
FINNHUB_API_KEY=  # https://finnhub.io/dashboard
TWELVEDATA_API_KEY=  # https://twelvedata.com/account/api-keys

# Government Economic Data APIs (Free)
FRED_API_KEY=  # https://fredaccount.stlouisfed.org/apikeys (Federal Reserve Economic Data)
BLS_API_KEY=  # https://www.bls.gov/developers/home.htm (Bureau of Labor Statistics - optional, enhances rate limits)
# Note: US Treasury API requires no key - completely free and open

# ============= ALTERNATIVE DATA APIS =============
# News & Sentiment APIs
NEWSAPI_KEY=  # https://newsapi.org/register (General news)
MARKETAUX_KEY=  # https://www.marketaux.com/ (Financial news with sentiment)
# Note: Finnhub and AlphaVantage keys above also provide news

# Social Media APIs
TWITTER_BEARER_TOKEN=  # https://developer.twitter.com/en/portal/dashboard
REDDIT_CLIENT_ID=  # https://www.reddit.com/prefs/apps
REDDIT_CLIENT_SECRET=  # Reddit app secret
STOCKTWITS_TOKEN=  # https://stocktwits.com/developers

# Weather & Environmental APIs
OPENWEATHER_API_KEY=  # https://openweathermap.org/api (Weather data for commodities)
NOAA_API_KEY=  # https://www.ncdc.noaa.gov/cdo-web/token (US weather data - optional)

# Commodity & Energy APIs
QUANDL_API_KEY=  # https://www.quandl.com/tools/api (Now Nasdaq Data Link)
EIA_API_KEY=  # https://www.eia.gov/opendata/register.php (US Energy Information)
USDA_API_KEY=  # https://quickstats.nass.usda.gov/api (Agricultural data)

# Cryptocurrency APIs
# Note: CoinGecko doesn't require API key for basic usage
ETHERSCAN_API_KEY=  # https://etherscan.io/apis (Ethereum blockchain data)

# Global Economic Data APIs
OECD_API_KEY=  # https://data.oecd.org/ (OECD economic data - optional)
# Note: World Bank and ECB APIs don't require keys

# Alternative Data Platforms
GOOGLE_TRENDS_KEY=  # Google Trends API (if available)

# ============= PRODUCTION SECURITY =============
# Use these settings in production:
ENVIRONMENT=production
DEBUG=false
SECURE_SSL_REDIRECT=true
SESSION_COOKIE_SECURE=true
CSRF_COOKIE_SECURE=true
